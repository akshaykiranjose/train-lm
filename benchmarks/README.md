# Benchmarks

Attention-only latency, memory, prefill, and decode benchmarks belong here after the numerical baseline passes the short learning run. The first milestone intentionally contains no custom kernels or premature systems optimizations.

